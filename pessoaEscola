class pessoaEscola {
    constructor(nome, idade) {
        this.nome = nome;
        this.idade = idade;
    }

    exibirDados() {
        console.log(`Nome: ${this.nome}, Idade: ${this.idade}`);
    }
}

module.exports = pessoaEscola;
